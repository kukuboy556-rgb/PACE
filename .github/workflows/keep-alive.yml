name: Keep Backend Alive
on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render backend
        run: curl -s -o /dev/null -w "%{http_code}" https://pace-api.onrender.com/api/auth/me
